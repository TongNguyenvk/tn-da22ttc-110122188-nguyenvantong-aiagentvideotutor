# ============================================================
# WebReel Backend - Docker/Linux dependencies ONLY
# (No Windows-only packages: pywin32, pywinauto, pyautogui, etc.)
# ============================================================

# Core
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0
psutil>=5.9.0

# FastAPI backend
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12
websockets>=14.0
email-validator>=2.0.0
slowapi>=0.1.9

# Database
motor>=3.3.0
pymongo>=4.6.0

# Storage
boto3>=1.34.0
botocore>=1.34.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<4.1.0
google-auth>=2.0.0
# OAuth web flow for admin "Login with Google" button (Google Drive token).
# Workers don't need this — only the API generates auth URLs and exchanges
# codes. Worker keeps using the saved pickle.
google-auth-oauthlib>=1.2.0

# Browser automation (CDP-based, connects to Browserless Chrome)
browser-use>=0.12.0
playwright>=1.40.0

# Gemini AI (primary LLM)
google-genai>=1.0.0

# Audio & Media
edge-tts>=6.1.0
mutagen>=1.47.0

# Redis queue
redis>=5.0.0

# Slide pipeline (PPTX/PDF to video)
python-pptx>=1.0.0
pdf2image>=1.17.0
Pillow>=10.0.0

# Graph API Auth
msal>=1.31.0
